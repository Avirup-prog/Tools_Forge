# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9      # file upload support

# ── PDF tools ─────────────────────────────────────────────────────────────────
pdfplumber==0.11.0            # text/table extraction
pypdf==3.17.4                 # merge, split, encrypt, rotate
python-docx==1.1.2            # PDF→Word, Word→PDF helper
reportlab==4.2.0              # PDF generation (watermark, fill-sign)
Pillow==10.4.0               # image handling shared across PDF + Image
pdf2image==1.17.0             # PDF→image (uses poppler)
img2pdf==0.5.1                # Image→PDF


# ── Image tools ───────────────────────────────────────────────────────────────
# Pillow already listed above
rembg==2.0.57                 # AI background removal
opencv-python-headless==4.9.0.80   # upscale / image processing

# ── Media tools ───────────────────────────────────────────────────────────────
# ffmpeg-python wraps the system ffmpeg binary
ffmpeg-python==0.2.0
pydub==0.25.1

# ── Utilities ─────────────────────────────────────────────────────────────────
httpx==0.27.0                 # async HTTP (URL shortener)
python-slugify==8.0.4
aiofiles==23.2.1
pdf2docx
numpy<2
python-pptx
